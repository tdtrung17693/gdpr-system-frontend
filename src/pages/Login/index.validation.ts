const rules = {
  username: [
    {
      required: true,
      message: 'Please type in your username',
    },
  ],
  password: [{ required: true, message: 'Please type in your password' }],
};

export default rules;
